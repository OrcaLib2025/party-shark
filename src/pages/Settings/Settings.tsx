import React from 'react';

import cl from './Settings.module.scss';

export const Settings: React.FC = () => {
    return (
        <div className={cl['']}>
            Настройки
        </div>
    );
};
